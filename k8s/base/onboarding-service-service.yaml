apiVersion: v1
kind: Service
metadata:
  name: onboarding-service
  namespace: onboarding
spec:
  selector:
    app: onboarding-service
  ports:
    - name: http
      port: 8081
      targetPort: http
  type: ClusterIP

