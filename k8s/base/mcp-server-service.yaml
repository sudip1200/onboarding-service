apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: onboarding
spec:
  selector:
    app: mcp-server
  ports:
    - name: http
      port: 9092
      targetPort: http
  type: ClusterIP

